# ==================================================
# Observability Platform Integration Configuration
# ==================================================

# Datadog Configuration
DATADOG_API_KEY=your_datadog_api_key_here
DATADOG_SITE=datadoghq.com
DD_ENV=production
DD_SERVICE=dry-fruits-platform
DD_VERSION=1.0.0

# Splunk Configuration
SPLUNK_HEC_URL=https://http-inputs-your-instance.splunkcloud.com:8088/services/collector
SPLUNK_HEC_TOKEN=your_hec_token_here
SPLUNK_INDEX=main

# Elastic APM Configuration
ELASTIC_APM_SERVER_URL=https://your-deployment-id.apm.your-region.aws.cloud.es.io:443
ELASTIC_APM_SECRET_TOKEN=your_secret_token_here
ELASTIC_APM_ENVIRONMENT=production

# New Relic Configuration
NEW_RELIC_LICENSE_KEY=your_license_key_here
NEW_RELIC_APP_NAME=dry-fruits-platform

# Jaeger Configuration (if using external Jaeger)
JAEGER_ENDPOINT=http://your-jaeger-instance:14268/api/traces

# Prometheus Configuration (if using external Prometheus)
PROMETHEUS_REMOTE_WRITE_URL=https://prometheus-remote-write-endpoint

# Grafana Cloud Configuration
GRAFANA_CLOUD_USER=your_user_id
GRAFANA_CLOUD_API_KEY=your_api_key_here

# Slack Notifications
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK

# Email Notifications
SMTP_HOST=smtp.your-provider.com
SMTP_PORT=587
SMTP_USER=alerts@dryfruits.com
SMTP_PASSWORD=your_email_password

# PagerDuty Integration
PAGERDUTY_INTEGRATION_KEY=your_integration_key_here

# ==================================================
# Local Development Override (leave empty for prod)
# ==================================================
# Uncomment and set for local development
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
# JAEGER_ENDPOINT=http://localhost:14268/api/traces